#include <string>
#include <unordered_map>

int characterReplacement(std::string& s, int key)
{
    std::unordered_set<char> seen;
    int left = 0, right = 0, res = 0;
    size_t count{0};

    while (right < s.length())
    {
      if () 
      {
        
      }
    }

    return res;
}

int main()
{
    return 0;
}
